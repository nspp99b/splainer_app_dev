<% provide(:title, 'All Users') %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <%= render 'shared/user_info' %>
    </section>
    <section class="splain_form">
      <% provide(:button_text, 'Splain') %>
      <%= render 'shared/splain_form' %>
    </section>
  </aside>
  <div class="col-md-8">
      <h3>All Users</h3>
      <ul class="users">
        <%= render @users %>
      </ul>
  </div>
</div>
